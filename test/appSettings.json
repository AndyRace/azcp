{
  "Repository": {
    // NOTE: The main container URI COULD be included here or in an optional appSettings.secrets.json file:
    // e.g.
    // "BlobContainerUri": "https://{storage account name}.blob.core.windows.net/{container name}?{SAS query string}"

    // "UploadFolder": "upload",
    // "ArchiveFolder": "archive",

    // The Microsoft Azure Storage Data Movement Library underpins this application.
    // This tool follows the guidance found here: https://github.com/Azure/azure-storage-net-data-movement/

    // The following are used to update Microsoft.Azure.Storage.DataMovement.TransferManager.Configurations
    // See: https://docs.microsoft.com/en-us/dotnet/api/microsoft.azure.storage.datamovement.transferconfigurations?view=azure-dotnet
    // "BlockSize":  4096,
    // "ParallelOperations": 96,

    // The following are used to update the System.Net.ServicePointManage
    // See: https://docs.microsoft.com/en-us/dotnet/api/system.net.servicepointmanager
    // "DefaultConnectionLimit": 96,
    // "Expect100Continue": false,
    // "Recursive": "true",
    // "TransferCheckpointFilename": null
  },

  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  }
}